"""数据库工具（Day10 引入）。

当前为占位文件，Day10 将实现：
1. 读取 .env 中的 DB_HOST / DB_USER / DB_PASSWORD / DB_NAME；
2. 使用 pymysql 建立 MySQL 连接并封装查询；
3. 提供断言辅助：如查询物品"在库/认领"状态，与接口返回比对。
"""

# TODO(Day10): 实现 connect() / query() / 断言辅助函数
